name: CI
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup tmate sessio
      uses: mxschmitt/action-tmate@v3
      with:
        limit-access-to-actor: true
